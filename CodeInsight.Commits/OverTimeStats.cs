using System;
using System.Collections.Generic;
using System.Text;

namespace CodeInsight.Commits
{
    //TODO: COMMIT STATS over (chosen) time
    /*
     * COMMIT STATS over (chosen) time
     *
     * Number of commits - graph with per week and per day per that week commit count
     * Addition/Deletion graph
     */
    class OverTimeStats
    {
    }
}
